# Evidrai — Claim Check

A simple Streamlit MVP for assessing the evidential strength of a claim.

## Setup

1. Create a virtual environment
2. Install dependencies:

pip install -r requirements.txt

3. Add your API key to `.env`

4. Run:

streamlit run app.py